# flutter_datatable_column_draggable

Projeto simples e prático para utilizar DataTable para "arrastar e soltar" (drag & drop) as colunas, permitindo modificar a ordem que a coluna será exibida no DataTable.
